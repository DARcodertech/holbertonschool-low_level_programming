#include "main.h"
/**
 * add - fonction for calcul
 * @a: number to add
 * @b: number to add
 * Return: return a result
 */
int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
